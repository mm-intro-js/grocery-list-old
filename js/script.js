//geting all the items from grocery-list.(Gives an array of items)
var glocery - list = document.getElementsByTagName("li");


//grocery-list items.
var glocery - list = ["Milk", "Cheese", "Bread", "Ice-cream"];


//logging the items into console.
console.log(glocery - list);


//looping through the list
function gloceryitems() {
    for (var i = 0; i < grocery - list.length; i++)
        console.log()
};

// removing all items in the list
function removeGloceryList {
    var grocery - list = removeElementById(glocery - list)
};

alert = ("Bought all the gloceries")